# Minimum versions, not exact pins: these are the floors the code is written
# against and was verified on. Use `pip freeze > requirements.lock.txt` when
# you deploy, so production installs exactly what you tested.

# --- Web framework ---------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.20

# --- Data validation -------------------------------------------------------
pydantic>=2.10
email-validator>=2.2

# --- Database --------------------------------------------------------------
motor>=3.3
pymongo>=4.6

# --- Auth ------------------------------------------------------------------
pyjwt>=2.8
bcrypt>=4.0
google-auth>=2.35          # verifies Google Identity Services ID tokens

# --- Integrations ----------------------------------------------------------
anthropic>=0.40            # roster summaries, OCR, rule compilation
stripe>=11.0
httpx>=0.27                # Resend email API + image fetching for OCR

# --- Spreadsheet import ----------------------------------------------------
openpyxl>=3.1               # reads legacy roster .xlsx files
pymupdf>=1.24               # renders PDF pages to images for OCR
reportlab>=4.0              # builds the roster PDF attached to dispatch

# --- Config ----------------------------------------------------------------
python-dotenv>=1.0

# --- Development / testing -------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.24
